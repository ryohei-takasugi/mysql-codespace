{
  "name": "learn-rdbms",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "mysql", // Replace "app" with the actual service name from your docker-compose.yml
  "workspaceFolder": "/workspace" // Replace "/workspace" with the desired folder path
}